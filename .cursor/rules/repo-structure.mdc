---
description: Использовать это правило, когда нужно понимать **целевую структуру репозитория** и назначение каталогов. 
alwaysApply: true
---

> **Description**:  
> Использовать это правило, когда нужно понимать **целевую структуру репозитория** и назначение каталогов.  
> Фактические файлы и каталоги изначально отсутствуют и создаются по мере выполнения шагов.  
> В этом правиле описывается только верхний уровень структуры и некоторые ключевые файлы (кроме каталога `rules`, который раскрыт подробнее).  
> Детальная структура каждого каталога фиксируется в специализированных правилах.


# Целевая структура репозитория и назначение каталогов

## 📂 Целевая структура репозитория (верхний уровень)

```
.
├─ .cursor/rules/           # правила для агента
│ ├─ repo-structure.md        # этот файл
│ ├─ prd-rules.md             # правила оформления PRD
│ ├─ architecture-rules.md    # правила архитектурных документов
│ ├─ deploy-rules.md          # правила развертывания
│ ├─ dbml-openapi-rules.md    # правила создания *логической модели данных и спецификацию OpenAPI
│ ├─ tasks-rules.md           # правила правление задачами (декомпозиция, роли, критерии)
│ ├─ dev-backend-rules        # правила для разработки серверного сервиса на Go
│ ├─ dev-frontend-rules.mdc   # правила для разработки frontend на React
│ ├─ tspec-rules.md           # правила подготовки спецификаций для задач backend, frontend и devops
│ └─ ...
├─ app/                     # исходный код приложения (backend на Go, frontend на React)
│ ├─ backend/                 # сервис на Go (API, бизнес-логика)
│ ├─ frontend/                # React-приложение
│ └─ internal/                # общие пакеты Go (модели, utils)
├─ db/                      # схема данных и миграции PostgreSQL
│ ├─ migrations/              # миграции (SQL или Go-скрипты)
│ ├─ schema.dbml              # логическая модель данных
│ └─ ...
├─ infra/                   # инфраструктура: docker-compose, конфигурация Caddy
│ ├─ docker-compose.yml       # запуск сервисов и миграций
│ └─ ...
├─ docs/                    # проектные артефакты (prd, архитектура, деплой и др.)
│ ├─ specs/                   # каталог с детальными спецификациями
│ ├─ prd.md                   # Product Requirements Document
│ ├─ architecture.md          # архитектура решения
│ ├─ deployment.md            # стратегия развертывания
│ ├─ openapi.yaml             # API‑контур
│ └─ ...
├─ tasks/                   # каталог с задачами проекта
│ └─ tasks.yaml               # детальное описание и статусы задач проекта
└─ README.md                # описание проекта
```

---

## Назначение каталога `.cursor/rules/`

- `repo-structure.md` — текущее правило: структура репозитория и назначение каталогов.  
- `prd-rules.md` — правила оформления **PRD** (Product Requirements Document).  
- `architecture-rules.md` — правила подготовки **архитектурных документов**.  
- `dev-rules.md` — правила **разработки** (Go, React, миграции).  
- `deploy-rules.md` — правила **развертывания** (docker-compose, Caddy, TLS).  

---

## Как использовать
1. При создании новых артефактов или каталогов сверяться с этим файлом для соблюдения целевой структуры.  
2. При генерации специализированных документов переходить к соответствующему правилу.  
3. В верхнем уровне репозитория сохранять только ключевые каталоги и README.

## Правила исключения файлов (.gitignore)

Файл `.gitignore` в корне проекта должен содержать исключения для:

### Обязательные исключения:
- **Системные файлы**: `.DS_Store` (macOS), `Thumbs.db` (Windows)
- **Секреты и конфигурация**: `.env` (локальные переменные окружения)
- **Backend артефакты**: `app/backend/bin/`, `app/backend/dist/`
- **Frontend артефакты**: `app/frontend/node_modules/`, `app/frontend/dist/`
- **IDE файлы**: `.vscode/`, `.idea/`, `*.swp`

### Должны быть под VCS:
- **Лок-файлы зависимостей**: `go.sum`, `package-lock.json`
- **Примеры конфигурации**: `.env.example`
- **Исходный код**: все файлы в `app/backend/internal/`, `app/frontend/src/`